build: off
branches:
  only:
    - master
    - devel
environment:
  matrix:
    - nodejs_version: "8"
    - nodejs_version: "7"
    - nodejs_version: "6"
platform:
  - x86
  - x64
before_build:
  - "node -v"
  - "npm -v"
build_script:
  - "npm install"
before_test:
  - "npm run build"
test_script:
  - "npm run e2e"
