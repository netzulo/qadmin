{{#unless logged}}
  {{panel-errored errored=true titleText="forms route" messageError="Need to login first" }}
{{else}}
  {{panel-errored errored=false titleText="forms route" message="working like a charm!" }}
{{/unless}}

{{outlet}}
