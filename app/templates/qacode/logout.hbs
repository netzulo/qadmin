{{#unless logged}}
  {{panel-errored errored=false titleText="logout route" message="working like a charm like a charm because you are not logged!" }}
{{else}}
  {{panel-errored errored=false titleText="logout route" message="working like a charm!" }}
  {{#bs-button onClick=(action "logout")}}
    Logout
  {{/bs-button}}
{{/unless}}


{{outlet}}
